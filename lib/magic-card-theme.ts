export const FITSEHAT_MAGIC_CARD = {
  gradientFrom: "var(--magic-from)",
  gradientTo: "var(--magic-to)",
  gradientColor: "var(--magic-fill)",
  gradientOpacity: 0.7,
  gradientSize: 220,
} as const;
